import 'package:flutter/material.dart';

class INDEX {
  static ValueNotifier<int> course_rec = ValueNotifier(0);
  static ValueNotifier<int> index = ValueNotifier(0);
  static ValueNotifier<int> user_course_index = ValueNotifier(0);
  static ValueNotifier<int> mentor_course_index = ValueNotifier(0);

}
